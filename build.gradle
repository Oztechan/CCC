buildscript {
    apply from: 'appConf.gradle'
    repositories {
        google()
        jcenter()
        maven { url 'https://maven.fabric.io/public' }
    }
    dependencies {
        classpath classpaths.values()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}